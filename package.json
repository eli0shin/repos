{
  "name": "repos",
  "version": "1.1.1",
  "private": true,
  "description": "CLI tool for managing git repositories",
  "license": "MIT",
  "author": "@eli0shin",
  "type": "module",
  "scripts": {
    "build": "bun build src/cli.ts --compile --outfile repos",
    "dev": "bun run src/cli.ts",
    "typecheck": "bun tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "oxfmt --check",
    "format:fix": "oxfmt",
    "test": "GIT_EDITOR=true bun test",
    "test:watch": "GIT_EDITOR=true bun test tests/ --watch",
    "prepare": "husky"
  },
  "dependencies": {
    "@commander-js/extra-typings": "^14.0.0",
    "commander": "^14.0.2"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.2",
    "@changesets/cli": "^2.29.8",
    "@total-typescript/ts-reset": "^0.6.1",
    "@types/node": "^25.0.3",
    "bun-types": "^1.3.5",
    "eslint": "^9.39.2",
    "eslint-for-ai": "^1.0.12",
    "eslint-import-resolver-typescript": "^4.4.4",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "oxfmt": "^0.43.0",
    "typescript": "^6.0.2"
  },
  "lint-staged": {
    "*.{ts,js,mjs}": [
      "eslint --fix",
      "oxfmt --no-error-on-unmatched-pattern"
    ],
    "*.{json,md}": [
      "oxfmt --no-error-on-unmatched-pattern"
    ]
  }
}
