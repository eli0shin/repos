---
'repos': patch
---

Warn instead of failing when worktree setup copy or command steps fail, so `work` and `stack` still create usable worktrees. Also allow setup copy entries to recursively copy directories into new worktrees.
