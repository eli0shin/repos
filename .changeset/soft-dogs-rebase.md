---
'repos': minor
---

Make `repos rebase` rebase branches onto their recorded parents and recursively rebase child branches. Add `--only` with conflict-safe continuation, and deprecate `repos restack` as an alias.
